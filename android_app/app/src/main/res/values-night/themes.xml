<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Bokoa" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/light_color</item>  <!-- buttons + top light scroll -->
        <item name="colorPrimaryVariant">@color/debug</item> <!-- ?? -->
        <item name="colorOnPrimary">@color/debug</item> <!-- text on button + buttons light wen pressed -->
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/debug</item> <!-- ?? -->
        <item name="colorSecondaryVariant">@color/debug</item> <!-- ?? -->
        <item name="colorOnSecondary">@color/debug</item> <!-- ?? -->
        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/dark_color</item> <!-- top top bar -->
        <!-- Customize your theme here. -->
    </style>
</resources>